/* eslint-disable camelcase */
export interface CreateNaver {
  name: string;
  job_role: string;
  birthdate: string;
  admission_date: string;
  project: string;
  url: string;
}

export interface Naver {
  id: string;
  url: string;
  name: string;
  admission_date: string;
  job_role: string;
  birthdate: string;
  project: string;
}
